import 'package:flutter/material.dart';

class BlossomTabView extends StatelessWidget {
  const BlossomTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Blossom"));
  }
}
